#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: a function that prints the alphabet in lowercase
 *
 * Return: 0 (Success)
*/

int main(void)
{
	char i = 97

	while (i <= 122)
		putchar(i);
		i++;
	return (0);
}
